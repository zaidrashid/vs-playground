@model MVC4Playground.Models.GuestResponse

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Thanks</title>
</head>
<body>
    @{
        try
        {
            WebMail.SmtpServer = "smtp.exmaple.com";
        }
        catch
        {
            
        }
        }
    <div>
        <h1>Thank you @Model.Name!</h1>
        @if (Model.WillAttend == true)
        {
            @: Thanks! 
        }   
        else
        {
            @: Sorry...
        }
    </div>
</body>
</html>
